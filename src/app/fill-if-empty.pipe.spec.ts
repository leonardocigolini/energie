import { FillIfEmptyPipe } from './fill-if-empty.pipe';

describe('FillIfEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new FillIfEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
